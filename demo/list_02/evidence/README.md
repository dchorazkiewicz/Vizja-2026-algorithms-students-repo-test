# Curated evidence — List 02

This directory is reserved for concise, human-curated snapshots taken from verified CI runs.

The full latest machine-generated evidence is written automatically by GitHub Actions to `../generated/`.
